#pragma once

#include "system.hpp"

namespace eosio {
   template <typename T>
   class binary_tree {
      public: 
         binary_tree(std::initializer_list<T> l) {
            for
         }

      private:
   };
} // namespace eosio
